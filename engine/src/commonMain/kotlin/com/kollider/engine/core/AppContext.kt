package com.kollider.engine.core

/**
 * Platform-provided application context, typically used to resolve resources or services.
 *
 * Each platform must supply an appropriate implementation (e.g., Android `Context` wrapper).
 */
expect object AppContext
